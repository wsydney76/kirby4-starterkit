<blockquote>
    {{ block.text|raw }}
    {% if block.citation.isNotEmpty %}
        <cite class="mt-2 block font-normal not-italic">
            {{ block.citation|raw }}
        </cite>
    {% endif %}
</blockquote>
