name: Button (Inline Editing)
icon: bolt
label: "{{ text }} -> {{ link }}"
preview: fields
wysiwyg: true
tabs:
  content:
    fields:
      link:
        type: url
      text:
        type: text
  styles:
    fields:
      class:
        type: text
      id:
        type: text
